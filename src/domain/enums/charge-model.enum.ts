export enum ChargeModel {
    FLAT = 'flat',
    PER_SEAT = 'per_seat',
    PER_API_CALL = 'per_api_call',
    TIERED = 'tiered',
    PACKAGE = 'package',
    VOLUME = 'volume',
    GRADUATED = 'graduated',
}
