export enum MeterType {
    API_CALL = 'api_call',
    EVENT = 'event',
    REQUEST = 'request',
    STORAGE = 'storage',
    BANDWIDTH = 'bandwidth',
    CPU = 'cpu',
    GPU = 'gpu',
    CUSTOM = 'custom',
}
